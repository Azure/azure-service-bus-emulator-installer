{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run/Debug logic app la-service-bus-emulator",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureLogicAppsStandard.pickProcess}"
        }
    ]
}